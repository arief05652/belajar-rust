pub fn operator() {
    /* ARITMATIK
     * [ +, -, /, *, % ]
     *
     * ASSIGNMENT
     * [ +=. -=, /=, *=. %= ]
     *
     * COMPARISON
     * [ ==, !=, >=, <=, >, < ]
     *
     * LOGICAL
     * [ !, ||, && ]
     */
}
